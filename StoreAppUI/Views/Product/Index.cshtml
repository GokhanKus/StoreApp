@model ProductListViewModel

<div class="row">
	<div class="col-md-3">
		@await Component.InvokeAsync("CategoriesMenu")
		@await Component.InvokeAsync("ProductFilterMenu")
		<div products number="5"></div>@*LastestProductTagHelper HtmlAttributeName sayesinde son urunlerin sayisini burada belirtebiliyoruz*@
	</div>

	<div class="col-md-9">
		<div class="row my-3">
			@foreach (var product in Model.Products)
			{
				<partial name="_ProductCard" model="@product" />
				@* @await Html.PartialAsync("_ProductCard",product) *@
			}
		</div>
		<div class="btn-group" page-model="@Model.Pagination" page-action="Index" page-classes-enabled="true" page-class="btn" page-class-normal="btn-outline-dark" page-class-selected="btn-primary">
				
		</div>
	</div>
</div>

@* @section Footer {
	<div>
		Footer Section for Product Index.cshtml
	</div>
} *@
