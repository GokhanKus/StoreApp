@model List<Product>

@foreach(var product in Model)
{
	<div>
		<h3>@product.ProductName</h3>
		<p>@product.Price</p>
		<p>@product.CreatedTime</p>
		<a asp-action="Get" asp-route-id="@product.Id">Details</a>
	</div>
}
